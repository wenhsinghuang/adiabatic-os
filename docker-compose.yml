services:
  core:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./desktop/template:/workspace
      - adiabatic-data:/workspace/.adiabatic
    environment:
      - PORT=3000
      - SHELL_DIST=/app/shell-dist

volumes:
  adiabatic-data:
