

### ⚠️ 三大潛在挑戰 (The Hard Problems)

儘管架構很美，但在落地時你會遇到極硬的技術牆：

#### 1. The "Schema Drift" Nightmare (Schema 漂移與衝突)

- **情境：** App X 定義了 `Tasks` 表包含 `status` 欄位。App Y 也想用 `Tasks` 表，但它想要 `state` 欄位。
    
- **挑戰：** 當多個 AI 生成的 App 試圖操作同一個底層 Data Row 時，**Architect (A1)** 必須非常聰明地處理 Schema Migration 和 Compatibility。如果處理不好，底層數據會變得支離破碎，或者 App Y 會因為找不到欄位而崩潰。
    
- **建議：** 需要一個強大的 **"Virtual Schema Layer"** 或 **"Mapping Logic"**，讓 App Y 認為它在讀 `state`，但實際上是指向 `status`。
    

#### 2. The "Cold Start" & Migration (資料導入的恐怖谷)

- **情境：** 用戶上傳了 Notion Export。
    
- **挑戰：** 讓 LLM 解析 CSV/Markdown 並建立正確的 Relational Schema (SQL) 是非常困難的。Notion 的數據通常極度髒（混雜的 Select 選項、不一致的日期格式）。
    
- **風險：** 如果第一次 Import 的結果是「半成品」或「錯誤的關聯」，用戶會瞬間失去信任。
    
- **建議：** "Sugar-coating the broccoli" 的策略是對的，但技術上可能需要 **"Human-in-the-loop"** 的確認介面，讓用戶在 Import 過程中確認：「這看起來像是一個 Task 列表嗎？」
    

#### 3. Latency & Cost (延遲與成本)

- **情境：** **Context Stream (E2)** 需要持續寫入和 Embedding，**Architect (A1)** 需要寫 Code。
    
- **挑戰：** 這不是簡單的 CRUD App。每次生成 App 都是一次昂貴的 LLM 調用。如果用戶點擊 "Save as App" 需要等 30 秒，體驗會大打折扣。
    
- **建議：** 這種架構非常依賴未來的推理成本下降（GPT-4o mini / Claude Haiku 等級的模型將是主力）。

----


這是一份最終定案的 **Adiabatic OS 實體關係圖 (Entity-Relationship Mapping)**。

這張圖將我們討論過的 **權限模型**、**Agent 角色** 以及 **Meta/App/Content 分層** 全部整合在一起，定義了這個系統的物理定律。

---

### 🗺️ The Adiabatic OS Architecture

我們將系統中的實體分為三個維度：**資產 (Assets)**、**結構 (Structure)**、**智能 (Intelligence)**。

#### 1. The Substrate (底層基質 - Content Layer)

_這是用戶擁有的資產，獨立於 App 存在。即使刪除所有 App，這一層依然完整。_

- **E1. Data Rows (資料記錄):**
    
    - **實體:** LibSQL / SQLite Rows.
        
    - **定義:** 系統的「狀態」 (e.g., 具體的任務 "Buy Milk", 具體的日誌 "10:00 AM").
        
    - **權限屬性:** **Global Read** (預設共享), **Scoped Write** (受保護).
        
- **E2. Context Stream (情境流):**
    
    - **實體:** Time Series Logs + Vector Embeddings.
        
    - **定義:** 系統的「感知記憶」 (e.g., "User is focusing on coding").
        
    - **來源:** 由 **Data Connector (Infra)** 持續寫入。
        
- **E3. Live Docs (活性畫布):**
    
    - **實體:** MDX (Markdown + JSX).
        
    - **定義:** 非結構化思考的容器。它是 **Data Rows** 的消費者，透過嵌入 **Apps** 來與資料互動。
        

#### 2. The Structure (結構與介面 - App Layer)

_這是 AI 生成的模具，用來規範和操作底層基質。_

- **E4. Schema (資料定義):**
    
    - **實體:** SQL DDL (`CREATE TABLE`, Foreign Keys).
        
    - **定義:** 數據的形狀。
        
    - **歸屬:** 屬於 **App** (由 App 定義)，但存在於 **Global DB** 中。
        
- **E5. Apps (應用單元):**
    
    - **實體:** React Components (Vibe Code) + **Manifest (權限清單)**.
        
    - **定義:** **Schema 的 View + 互動 Logic** + in-app AI intellegence。
        
    - **特性:** **Disposable (可拋棄)**。它是 Schema 的操作介面，刪除 App 不會刪除 Data Rows。
        

#### 3. The Intelligence (操作者 - Meta Layer)

_這是驅動系統演化的 SaaS 服務。_

- **A1. The Architect (Builder):**
    
    - **職責:** **Create / Evolve**.
        
    - **操作對象:** 寫入 **E5 (Apps)** 和 **E4 (Schema)**。
        
- **A2. The Optimizer (Gardener):**
    
    - **職責:** **Maintain / Merge**.
        
    - **操作對象:** 讀取 **E2 (Context)**，重構 **E1 (Data Rows)** 和 **E4 (Schema)** (例如合併重複的 Table)。
        
- **A3. The Companion (Co-pilot):**
    
    - **職責:** **Assist / Chat**.
        
    - **操作對象:** 生成/修改 **E3 (Live Docs)**。
        
- **A4. In-app AI:**
    
    - **職責:** **Execute user app logic**.
        
    - **操作對象:** 觸發自動化。
    - 它是 **Vibe Code** 的一部分。
        

---

### 🔒 關鍵互動規則 (Interaction Rules)

這定義了 **App** 與 **Data** 之間的物理定律（Runtime Guard 執行的規則）：

#### 規則 A: Namespace Ownership (寫入權限)

> **"App 只能修改自己創造的宇宙。"**

- **App X** 創建了 Table `app_x_tasks`。
    
- **App X** 擁有對 `app_x_tasks` 的 **Read/Write** 權限。
    
- **App Y** 想要修改 `app_x_tasks`？ -> **Access Denied** (除非 App X 明確開放 API 或 User 授權)。
    

#### 規則 B: Universal Readability (讀取權限)

> **"知識是共享的，但寫入是私有的。"**

- **App Y (Dashboard)** 可以執行 `SELECT * FROM app_x_tasks`。
    
- **目的:** 為了實現 **Unified Data Layer**，讓 Dashboard 能聚合所有 App 的數據。
    
- **例外:** 敏感數據 (Health, Finance) 需要 User Grant (彈窗授權)。
    

#### 規則 C: The "Promote" Lifecycle (演化路徑)

> **"從文檔湧現出應用。"**

1. **Ephemeral:** 用戶在 **Live Doc (E3)** 裡，由 **Companion (A3)** 生成了一個臨時組件。
    
2. **Solidify:** 用戶點擊 "Save as App"。
    
3. **Permanent:** **Architect (A1)** 接手，將其封裝為 **App (E5)**，並在底層建立正式的 **Schema (E4)**。
    

---

### 📐 架構層級總結 (The Stack)

| **層級 (Layer)**    | **實體 (Entity)**      | **角色 (Role)**      | **變動頻率**   |
| ----------------- | -------------------- | ------------------ | ---------- |
| **Intelligence**  | **Agents (AI)**      | **Operator (駕駛員)** | 極快 (隨時思考)  |
| **App Layer**     | **Apps & Schema**    | **Structure (模具)** | 中等 (隨需求演化) |
| **Content Layer** | **Data Rows & Logs** | **Substrate (基質)** | 極快 (隨時寫入)  |
| **Infra Layer**   | **LibSQL & Runtime** | **Physics (物理定律)** | 極慢 (穩定基礎)  |
|                   |                      |                    |            |

這張圖表現在是完全 Self-Consistent (自洽) 的。

它解決了孤島問題 (Global Read)，解決了安全問題 (Scoped Write)，並解決了演化問題 (Agent Roles)。